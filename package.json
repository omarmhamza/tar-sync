{
  "name": "tar-sync",
  "version": "1.0.0",
  "description": "Tar-Sync automates tar archive creation and synchronization with directory changes.",
  "main": "index.js",
  "scripts": {
    "start": "node --watch --loader=ts-node/esm ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Omar Hamza",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.19",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@types/tar-fs": "^2.0.4",
    "chokidar": "^3.6.0",
    "tar-fs": "^3.0.5"
  }
}
